name: Build and Release Chrome Extension (dist-chrome.zip only)

on:
  push:
    tags:
      - 'v*'  # Triggers on tag push like v1.0.0

permissions:
  contents: write  # Required to create releases

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v3

      - name: 🟦 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install dependencies
        run: npm install

      - name: 🛠 Build the extension
        run: npm run build

      - name: 📁 Prepare extension for packaging
        run: |
          mkdir ai-website-assistant
          cp -r dist/* ai-website-assistant/
          cp public/manifest.json ai-website-assistant/
          cp -r public/* ai-website-assistant/ || true

      - name: 📦 Zip extension as dist-chrome.zip
        run: zip -r dist-chrome.zip ai-website-assistant

      - name: 🚀 Create GitHub release with zip
        uses: softprops/action-gh-release@v1
        with:
          files: dist-chrome.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
